enum ConvertMode {
  JSONtoString,
  StringtoJSON,
}

enum ColorScheme {
  Dark = "dark",
  Light = "light",
}

enum IconVariant {
  beautify,
  copy,
  delete,
  error,
  uglify,
}

export { ConvertMode, ColorScheme, IconVariant };
