enum ConvertMode {
  JSONtoString,
  StringtoJSON,
}

enum Formats {
  json,
  string,
}

enum ColorScheme {
  Dark = "dark",
  Light = "light",
}

enum IconVariant {
  beautify,
  copy,
  delete,
  error,
  uglify,
}

export { ConvertMode, ColorScheme, IconVariant, Formats };
